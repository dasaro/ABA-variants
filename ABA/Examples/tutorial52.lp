head(r1, ca).
body(r1, a).

head(r2, ca).
body(r2, b).

head(r3, cb).
body(r3, a).

head(r4, cc).
body(r4, d).

head(r5, cd).
body(r5, c).

head(r6, ce).
body(r6, c).

head(r7, ce).
body(r7, d).

head(r8, cf).
body(r8, e).

assumption(a).
contrary(a, ca).

assumption(b).
contrary(b, cb).

assumption(c).
contrary(c, cc).

assumption(d).
contrary(d, cd).

assumption(e).
contrary(e, ce).

assumption(f).
contrary(f, cf).
